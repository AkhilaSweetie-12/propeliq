# DevOps Security Review Report

## Review Metadata
| Attribute | Value |
|-----------|-------|
| Review ID | SECREV-20260416-000002 |
| Review Date | 2026-04-16 |
| Scope | Full DevOps (pipeline + IaC + secrets) |
| Artifacts Reviewed | .github/workflows/gcp-cloudrun-ci-cd.yml, .propel/context/iac/gcp/terraform/, scripts/gcp-cicd-bootstrap.sh |
| Status | Complete |

## Overall Verdict
Status: APPROVED WITH CONDITIONS

Risk Level: LOW

Summary: The pipeline applies OIDC-only auth, SHA-pinned third-party actions, secret and vulnerability scanning, and environment-gated production deploys. No critical findings were identified. Three conditions must be closed before first production deployment: WORKLOAD_IDENTITY_PROVIDER placeholder, `--allow-unauthenticated` on the prod service, and missing concurrency controls.

## Key Metrics
| Metric | Count | Status |
|--------|-------|--------|
| Critical Findings | 0 | PASS |
| High Findings | 1 | FAIL |
| Medium Findings | 2 | INFO |
| Low Findings | 2 | INFO |

---

## Pipeline Security Assessment

### Secret Handling
| Check ID | Check | Status | Finding |
|----------|-------|--------|---------|
| PIPE-001 | No inline secrets in pipeline code | PASS | All credentials via env vars and OIDC |
| PIPE-002 | Secrets from platform secret store | PASS | GitHub environment secrets used |
| PIPE-003 | Secrets masked in logs | PASS | OIDC tokens never echoed |
| PIPE-004 | OIDC for cloud authentication | PASS | google-github-actions/auth with WIF |

### Workflow Security
| Check ID | Check | Status | Finding |
|----------|-------|--------|---------|
| WKFL-001 | Workflow permissions minimized | PASS | Top-level: contents read, id-token write. security job narrows to read + security-events only |
| WKFL-002 | Actions pinned to commit SHA | PASS | All 6 third-party actions pinned to full SHA with version comment |
| WKFL-003 | Third-party actions reviewed | PASS | actions, google-github-actions, gitleaks, aquasecurity — all established maintainers |
| WKFL-004 | Environment protection rules | WARN | Rules must be configured in GitHub repository settings (not enforceable in YAML alone) |

### Security Gates
| Check ID | Check | Status | Finding |
|----------|-------|--------|---------|
| GATE-001 | SAST configured and blocking | WARN | CodeQL not yet wired; trivy fs covers filesystem/deps only. Add CodeQL job or equivalent SAST. |
| GATE-002 | Secrets detection configured and blocking | PASS | Gitleaks with full history |
| GATE-003 | Container scanning configured | PASS | Trivy fs scan on CRITICAL/HIGH with exit-code 1 |
| GATE-004 | Security gates cannot be bypassed | PASS | security job required before build-and-push via needs |

---

## Findings Detail

### High Findings
| Finding ID | Category | Description | Remediation | Status |
|------------|----------|-------------|-------------|--------|
| HIGH-001 | Network exposure | `--allow-unauthenticated` on prod Cloud Run service exposes it publicly without identity checks. | Remove `--allow-unauthenticated` for prod. Add `--no-allow-unauthenticated` and route traffic through IAP or a load balancer with auth. | Open |

### Medium Findings
| Finding ID | Category | Description | Remediation | Status |
|------------|----------|-------------|-------------|--------|
| MED-001 | Placeholder not replaced | `WORKLOAD_IDENTITY_PROVIDER` is still the scaffold value `projects/123456789012/...`. Pipeline will fail auth on first run. | Run bootstrap script, copy output value, replace in workflow env section. | Open |
| MED-002 | Concurrency not controlled | Multiple concurrent workflow runs can trigger overlapping `terraform apply` or `gcloud run deploy` for the same environment. | Add `concurrency: group: deploy-${{ github.ref }}-${{ matrix.env }}, cancel-in-progress: false` per deploy job. | Open |

### Low Findings
| Finding ID | Category | Description | Remediation |
|------------|----------|-------------|-------------|
| LOW-001 | SAST gap | No CodeQL job in main CI workflow. Trivy + Gitleaks cover secrets and dependencies but not source code logic. | Add a CodeQL job to the security stage targeting Python. |
| LOW-002 | Image tag mutability | Image tagged with git SHA which is immutable, but the Artifact Registry repo has no retention/cleanup policy defined. | Configure cleanup policies in Artifact Registry to limit image accumulation. |

---

## Conditions for Approval Before Production
- [ ] HIGH-001: Replace `--allow-unauthenticated` with `--no-allow-unauthenticated` on prod deploy job.
- [ ] MED-001: Replace `WORKLOAD_IDENTITY_PROVIDER` placeholder after running bootstrap.
- [ ] MED-002: Add concurrency controls to deploy-dev and deploy-prod jobs.

## Recommendations
1. Add CodeQL Python analysis job to close LOW-001 (P0 before prod go-live).
2. Set Artifact Registry cleanup policy to retain last 10 images per service (P1).
3. Configure GitHub environment protection rules for prod with 2 required reviewers and branch restriction to main (P1).
