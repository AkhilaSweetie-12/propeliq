# name: Infra Pipeline

# on:
#   pull_request:
#     paths:
#       - "infra/**"
#       - ".github/workflows/infra-pipeline.yml"
#   push:
#     branches:
#       - main
#       - feat/devops
#     paths:
#       - "infra/**"
#       - ".github/workflows/infra-pipeline.yml"
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: "Target environment for manual apply"
#         required: true
#         default: "dev"
#         type: choice
#         options:
#           - dev
#       apply:
#         description: "Run apply for selected environment"
#         required: true
#         default: false
#         type: boolean

# permissions:
#   contents: read
#   id-token: write

# env:
#   TF_VERSION: "1.8.5"
#   TF_WORKING_DIR: "infra"

# jobs:
#   validate:
#     name: Validate Terraform
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: ${{ env.TF_WORKING_DIR }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Terraform Format Check
#         run: terraform fmt -check -recursive

#       - name: Terraform Init (No Backend)
#         run: terraform init -backend=false

#       - name: Terraform Validate
#         run: terraform validate

#   security_scan:
#     name: Security Scan (tfsec)
#     runs-on: ubuntu-latest
#     needs: validate
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Run tfsec
#         uses: aquasecurity/tfsec-action@v1.0.3
#         with:
#           working_directory: infra
#         continue-on-error: true

#   plan:
#     name: Plan (dev)
#     runs-on: ubuntu-latest
#     needs:
#       - validate
#       - security_scan
#     if: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER != '' && vars.GCP_SERVICE_ACCOUNT != '' }}
#     defaults:
#       run:
#         working-directory: ${{ env.TF_WORKING_DIR }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Authenticate to GCP (OIDC)
#         uses: google-github-actions/auth@v2
#         with:
#           workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
#           service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

#       - name: Setup gcloud SDK
#         uses: google-github-actions/setup-gcloud@v2

#       - name: Terraform Init
#         run: |
#           if [ -n "${{ vars.TF_STATE_BUCKET }}" ]; then
#             terraform init \
#               -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
#               -backend-config="prefix=propeliq/dev"
#           else
#             terraform init -backend=false
#           fi

#       - name: Terraform Plan (dev)
#         run: terraform plan -input=false -var-file=terraform.tfvars

#   apply_dev:
#     name: Apply (dev on push)
#     runs-on: ubuntu-latest
#     needs: plan
#     if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.GCP_WORKLOAD_IDENTITY_PROVIDER != '' && vars.GCP_SERVICE_ACCOUNT != '' }}
#     environment: dev
#     defaults:
#       run:
#         working-directory: ${{ env.TF_WORKING_DIR }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Authenticate to GCP (OIDC)
#         uses: google-github-actions/auth@v2
#         with:
#           workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
#           service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

#       - name: Setup gcloud SDK
#         uses: google-github-actions/setup-gcloud@v2

#       - name: Terraform Init
#         run: |
#           if [ -n "${{ vars.TF_STATE_BUCKET }}" ]; then
#             terraform init \
#               -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
#               -backend-config="prefix=propeliq/dev"
#           else
#             terraform init -backend=false
#           fi

#       - name: Terraform Apply (dev)
#         run: terraform apply -auto-approve -input=false -var-file=terraform.tfvars

#   apply_manual:
#     name: Apply (manual)
#     runs-on: ubuntu-latest
#     needs: plan
#     if: ${{ github.event_name == 'workflow_dispatch' && inputs.apply == true && vars.GCP_WORKLOAD_IDENTITY_PROVIDER != '' && vars.GCP_SERVICE_ACCOUNT != '' }}
#     environment: ${{ inputs.environment }}
#     defaults:
#       run:
#         working-directory: ${{ env.TF_WORKING_DIR }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Authenticate to GCP (OIDC)
#         uses: google-github-actions/auth@v2
#         with:
#           workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
#           service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

#       - name: Setup gcloud SDK
#         uses: google-github-actions/setup-gcloud@v2

#       - name: Terraform Init
#         run: |
#           if [ -n "${{ vars.TF_STATE_BUCKET }}" ]; then
#             terraform init \
#               -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
#               -backend-config="prefix=propeliq/${{ inputs.environment }}"
#           else
#             terraform init -backend=false
#           fi

#       - name: Terraform Apply (selected environment)
#         run: |
#           if [ "${{ inputs.environment }}" = "dev" ]; then
#             terraform apply -auto-approve -input=false -var-file=terraform.tfvars
#           elif [ "${{ inputs.environment }}" = "staging" ]; then
#             terraform apply -auto-approve -input=false -var-file=terraform.staging.tfvars
#           else
#             terraform apply -auto-approve -input=false -var-file=terraform.prod.tfvars
#           fi
